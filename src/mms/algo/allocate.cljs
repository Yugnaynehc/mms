(ns mms.algo.allocate
  (:require
   [mms.models.process-queue :as p]
   [mms.models.section-table :as s]))

(defn first-fit
  "首次适应算法"
  []
  )

(defn best-fit
  "最佳适应算法"
  [])

(defn worst-fit
  "最坏适应算法"
  [])

